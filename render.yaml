databases:
  - name: alrahma-database
    plan: free
    databaseName: alrahmadb

services:
  # .NET Backend API
  - type: web
    name: alrahma-backend
    env: dotnet
    plan: free
    region: oregon
    branch: main
    rootDirectory: AlRahmaBackend
    buildCommand: |
      dotnet restore
      dotnet publish -c Release -o output
    startCommand: dotnet output/AlRahmaBackend.dll
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: DOTNET_CLI_TELEMETRY_OPTOUT
        value: 1
      - key: ConnectionStrings__DefaultConnection
        fromDatabase:
          name: alrahma-database
          property: connectionString
    healthCheckPath: /health

  # React Frontend - FIXED TYPE
  - type: static_site
    name: alrahma-frontend
    plan: free
    region: oregon
    branch: main
    rootDirectory: Al-Rahma-front
    buildCommand: |
      npm install
      npm run build
    publishDirectory: dist
    envVars:
      - key: VITE_API_BASE_URL
        fromService:
          name: alrahma-backend
          type: web
          property: url